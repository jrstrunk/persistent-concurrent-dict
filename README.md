# persistent_concurrent_dict
Add as a git dependency in your gleam.toml file:
```toml
[dependencies]
persistent_concurrent_dict = { git = "https://github.com/jrstrunk/persistent-concurrent-dict.git", ref = "main" }
```
